const { Transaction, Double } = require('mongodb');
const mongoose = require('mongoose');

// Define the CredentialsSchema
const CostCentreCategoriesSchema = new mongoose.Schema({
    // Add more fields as needed
});

module.exports = {
    CostCentreCategoriesSchema
};
console.log('CostCentreCategoriesSchema:', CostCentreCategoriesSchema);


